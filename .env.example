# Frontend Environment Variables

# Backend API URL (only needed in production)
# In development, the Vite proxy handles API calls
VITE_BACKEND_URL=https://e621-guessr-backend.railway.app

# Optional: Analytics or tracking IDs
# VITE_GA_TRACKING_ID=your-google-analytics-id
# VITE_SENTRY_DSN=your-sentry-dsn

# Copy this file to .env and fill in your actual URLs
# Never commit .env files to version control!